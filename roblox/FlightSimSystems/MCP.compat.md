# MCP compatibility

The production MCP module is `MCP.v02.lua`. The legacy `MCP.lua` implementation is intentionally retired because it could write guidance outputs directly and diverged from the current architecture.
